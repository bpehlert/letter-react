import styled from "styled-components";

const DateButton = styled.button`
  background: none;
  border: none;
  text-align: left;
  outline: none;
  padding: 0 0 10px 0;
  font-size: 1em;

  display: flex;
  cursor: pointer;
`;

export default DateButton;
