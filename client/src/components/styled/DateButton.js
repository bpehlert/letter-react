import styled from "styled-components";

const DateButton = styled.button`
  background: none;
  border: none;
  text-align: left;
  text-emphasis: none;
  display: flex;
`;

export default DateButton;
